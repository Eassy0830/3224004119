# 结对项目：小学四则运算题目生成器

> 提交前请替换：姓名 A（学号）、姓名 B（学号）、GitHub 项目地址。

## PSP 2.1 记录（分钟）

| 阶段 | 预估 | 实际 |
|---|---:|---:|
| 需求分析与设计 | 45 | 待填写 |
| 表达式与分数运算实现 | 100 | 待填写 |
| 命令行与文件读写 | 45 | 待填写 |
| 单元测试与修复 | 80 | 待填写 |
| 性能分析与优化 | 40 | 待填写 |
| 文档/博客 | 50 | 待填写 |
| 合计 | 360 | 待填写 |

## 设计实现

程序按职责分为三层，并集中在 `Myapp.js`：`Rat` 与 `Expr` 实现精确分数和表达式树；`Generator`、`parse`、`combine` 处理生成、解析和合法性；`generate`、`grade`、`main` 负责参数与文件读写。`tests/test.js` 为回归测试。

```text
CLI → generate/grade → Expr 树
                       ├─ Rat/BigInt 精确求值
                       ├─ render 输出
                       └─ canonical 去重
```

生成器先随机分配 1~3 个运算符给左右子树，再合并。合并减法时拒绝负数；合并除法时拒绝零除数及非真分数结果。去重键只对每一个 `+` 或 `×` 节点交换左右孩子，并保留树形：这精确符合“可进行有限次交换”的要求，既能把 `3+(2+1)` 和 `1+2+3` 识别为重复，也不会把 `1+2+3` 与 `3+2+1` 误判为重复。

## 关键代码说明

`combine` 是唯一的二元运算构造入口，集中维护题目合法性。`Rat` 通过最大公约数约分，避免浮点误差。`parse` 按“加减层 → 乘除层 → 原子”解析，保证判题时遵循优先级和括号。

## 测试运行

已提供 10 个自动化测试用例，覆盖：分数/带分数格式化、分数加法、优先级、括号、负数减法拒绝、真分数除法、交换律去重、非结合律去重、生成器约束、正确/错误答案统计。命令：`node tests/test.js`。

另可手工测试：`-n 10 -r 10`、`-n 10000 -r 10`、缺少 `-r`、`-r 1`、答案缺失、带分数答案、带括号题目、`*`/`/` 运算符、除数为零、错误编号答案。

## 效能分析

性能热点是随机生成时的表达式构造和规范键计算。优化思路是：使用 `BigInt` 精确约分、以字符串键的哈希集合做到平均 O(1) 去重、限制每题至多三层运算符。实测命令为 `Measure-Command { node Myapp.js -n N -r 10 }`：10、100、1000、10000 题分别为 0.08、0.07、0.07、0.13 秒。性能图见 `性能分析.svg`；生成 10000 题满足需求 8。记录本项实际花费时间到 PSP 表。

## 项目小结与结对感受

（由两位同学结合真实分工填写。建议分别说明：需求澄清、测试发现的问题、协作中对方的具体优点，以及下次希望改进的沟通/提交节奏。）
